# Pixel Realms — server env vars
#
# If SUPABASE_URL + SUPABASE_SERVICE_KEY are both set, accounts persist in
# Supabase. Otherwise falls back to data/accounts.json on disk.
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Optional — defaults to 2567.
PORT=2567
