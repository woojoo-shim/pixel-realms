# Pixel Realms — client env vars
#
# Set VITE_SERVER_URL on Vercel to your deployed game server's wss:// URL.
# Example:
#   VITE_SERVER_URL=wss://pixel-realms-server.fly.dev
#
# If unset, the client falls back to ws://<current-host>:2567 (local dev).
VITE_SERVER_URL=
